import { StyleSheet } from 'react-native'

const ListItemSS = StyleSheet.create({
    card : {
        borderColor: 'black',
        borderWidth: 1,
        borderRadius:3,
        shadowColor: '#ccc',
        padding:5,
        marginVertical: 3,
        height: 50,
        justifyContent: 'center'
    }
});

export default ListItemSS